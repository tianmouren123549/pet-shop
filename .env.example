## Frontend environment examples
## Copy to `.env.local` and adjust as needed.

# true = use mock API in `src/utils/request.js`
# false = call real backend API endpoints
VITE_USE_MOCK=true

# backend base URL when VITE_USE_MOCK=false
VITE_API_BASE_URL=http://localhost:8080

