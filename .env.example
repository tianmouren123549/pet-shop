## Frontend environment examples
## Copy to `.env.local` and adjust as needed.

# API 模式：在 src/utils/request.js 顶部 USE_MOCK；false 为真实后端（默认），true 为内置离线数据（未接环境变量）。

# 可选：API 根地址。留空则请求相对路径 /api，由 Vite 代理到 localhost:8080（见 vite.config.js）。
# 若直连后端可设为完整 URL，变量名须与代码一致：VITE_API_BASE
# VITE_API_BASE=http://localhost:8080
